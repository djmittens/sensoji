package me.ngrid.sensoji

/**
  * Streams api is loosely based on high level kafka streams api, but abstracts details of where the data is read from
  * and written too, which allow for powerful flexibility for testing, and experimentation.
  */
package object streams {
}
